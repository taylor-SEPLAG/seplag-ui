export interface {{pascalCase name}}Request {
  id?: number,
  descricao?: string;
  observacao?: string;
  dataInicio?: string;
  dataFim?: string;
  filtro: string;
  page?: number,
  rows?: number
}