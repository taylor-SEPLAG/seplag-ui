import {describe,expect,it} from "vitest";
import {controleVagasStore} from "./controleVagasStore";
import {efetivarRemocaoExterna} from "./remocoes";
import type {MovimentacaoFuncional,Vaga} from "./types";
const responsavel={usuarioId:"TESTE",nome:"Teste",unidade:"SEPLAG",perfil:"Teste",data:"2026-07-27"};
describe("remoção externa",()=>{it("libera a vaga de origem e ocupa uma vaga compatível no destino",()=>{const base=controleVagasStore.getState(),ocupacao=base.ocupacoes.find(o=>o.situacao==="ATIVA"&&base.vagas.some(v=>v.id===o.vagaId&&v.estado==="OCUPADA"))!,origem=base.vagas.find(v=>v.id===ocupacao.vagaId)!;const destino:Vaga={...origem,id:"VAG-TESTE-DESTINO-00001",codigo:"VAG-TESTE-DESTINO-00001",estado:"DISPONIVEL",orgaoTitular:"SEFAZ",historico:[]};const mov:MovimentacaoFuncional={id:"MOV-REM-EXT-TESTE",tipo:"REMOCAO",subtipo:"REMOCAO_EXTERNA",situacao:"PUBLICADA",pessoaId:ocupacao.pessoaId,vinculoId:ocupacao.vinculoId,ocupacaoOrigemId:ocupacao.id,vagaOrigemId:origem.id,vagaDestinoId:destino.id,orgaoOrigem:ocupacao.orgaoLotacao,unidadeOrigem:ocupacao.unidadeLotacao??ocupacao.orgaoLotacao,orgaoDestino:"SEFAZ",unidadeDestino:"SEFAZ — Unidade",processo:"PROC-TESTE",ato:"ATO-TESTE",dataSolicitacao:"2026-07-27",dataEfeito:"2026-08-01",criadoEm:"2026-07-27",atualizadoEm:"2026-07-27",registradoPor:responsavel,detalhesRemocao:{abrangencia:"EXTERNA",fundamento:"A_PEDIDO",orgaoDestino:"SEFAZ",unidadeOrigem:ocupacao.orgaoLotacao,unidadeDestino:"SEFAZ — Unidade",vagaDestinoId:destino.id,alteraLotacao:true,alteraExercicio:true,pedidoOuOficio:"Pedido",situacaoOperacional:"AGENDADA"},comprometimentos:{},validacoes:[],fases:[],historico:[]};const r=efetivarRemocaoExterna({...base,vagas:[...base.vagas,destino],movimentacoesFuncionais:[mov,...base.movimentacoesFuncionais]},mov.id);expect(r.erro).toBeUndefined();expect(r.state.vagas.find(v=>v.id===origem.id)?.estado).toBe("DISPONIVEL");expect(r.state.vagas.find(v=>v.id===destino.id)?.estado).toBe("OCUPADA");expect(r.state.ocupacoes.find(o=>o.id===ocupacao.id)?.situacao).toBe("ENCERRADA");expect(r.state.ocupacoes.some(o=>o.vagaId===destino.id&&o.vinculoId===ocupacao.vinculoId&&o.situacao==="ATIVA")).toBe(true)})});